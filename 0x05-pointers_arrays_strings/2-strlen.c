#include "main.h"
#include <string.h>
/**
 * _strlen - returns the length of a string
 * @s: the string
 * Return: the length of s
 */
int _strlen(char *s)
{
	int length = 0;

	length = strlen(s);
	return (length);
}
